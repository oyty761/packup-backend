<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.packupbackend.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.packupbackend.entity.User">
        <id property="id" column="id"/>
        <result property="openId" column="open_id"/>
        <result property="unionId" column="union_id"/>
        <result property="sessionKey" column="session_key"/>
        <result property="nickname" column="nickname"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="phone" column="phone"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="lastLoginIp" column="last_login_ip"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.packupbackend.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (open_id, union_id, session_key, nickname, avatar_url, phone, 
                         username, password, status, last_login_time, last_login_ip, 
                         created_time, updated_time)
        VALUES (#{openId}, #{unionId}, #{sessionKey}, #{nickname}, #{avatarUrl}, #{phone},
                #{username}, #{password}, #{status}, #{lastLoginTime}, #{lastLoginIp},
                #{createdTime}, #{updatedTime})
    </insert>

    <select id="selectById" parameterType="Long" resultMap="UserResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectByOpenId" parameterType="String" resultMap="UserResultMap">
        SELECT * FROM user WHERE open_id = #{openId}
    </select>

    <select id="selectByUsername" parameterType="String" resultMap="UserResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT * FROM user
    </select>

    <select id="selectByStatus" parameterType="Integer" resultMap="UserResultMap">
        SELECT * FROM user WHERE status = #{status}
    </select>

    <update id="update" parameterType="com.example.packupbackend.entity.User">
        UPDATE user
        SET open_id = #{openId}, union_id = #{unionId}, session_key = #{sessionKey},
            nickname = #{nickname}, avatar_url = #{avatarUrl}, phone = #{phone},
            username = #{username}, password = #{password}, status = #{status},
            last_login_time = #{lastLoginTime}, last_login_ip = #{lastLoginIp},
            updated_time = #{updatedTime}
        WHERE id = #{id}
    </update>

    <update id="updateLastLogin">
        UPDATE user
        SET last_login_time = NOW(), last_login_ip = #{ip}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <select id="existsByUsername" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM user WHERE username = #{username}
    </select>

    <select id="existsByOpenId" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM user WHERE open_id = #{openId}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM user
    </select>

</mapper>
